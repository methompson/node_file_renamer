## TODOs:

* Confirm that each operation can be canceled
* Add string version of `getNumberWithFileUpdates` that shows previews when text is updated
* Consolidate case operations into a single operation with a dropdown to select the case type
* Consolidate insert operations into a single operation with a dropdown to select start or end
* Consolidate Trim start and end into a single operation with a dropdown to select start or end
* List operations.
  * Show list in reverse order
  * Give option to delete that single operation or all ops after the selected op
  * Let user delete any operation
  * Show changed file name examples for the ops up to that point in the list